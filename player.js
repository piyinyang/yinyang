import { topLayer, topLayer2, topLayer3, slime, spike} from "./GameScene.js";


export default class Player {
    // Cria a classe do player
    
    constructor(scene, x, y, jogador){
        this.scene = scene;
        const anims = scene.anims;

        
    }
    
}